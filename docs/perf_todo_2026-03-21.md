# Performance TODO Sprint (2026-03-21)

## Scope
Improve Ferrumyx workflow performance and correctness for ingestion, ranking, database/query paths, and documentation alignment.

## Task Board

- [x] P0 Provider refresh parallelization
  Owner: Sub-agent Beauvoir (`019d0f72-46c1-7601-9acc-5d94579a2078`)
  Files: `crates/ferrumyx-ranker/src/lib.rs`
  Success criteria: provider refresh no longer serial per provider per gene; report counters remain correct.

- [x] P0 Bound in-memory provider caches
  Owner: Sub-agent Beauvoir (`019d0f72-46c1-7601-9acc-5d94579a2078`)
  Files: `crates/ferrumyx-ranker/src/lib.rs`
  Success criteria: caches have max-size and TTL semantics; no unbounded growth.

- [x] P0 Heavy-lane async correctness + telemetry
  Owner: Sub-agent Plato (`019d0f72-46fa-7431-908d-de0707d2c87f`)
  Files: `crates/ferrumyx-ingestion/src/pipeline.rs`
  Success criteria: heavy enrichment outcomes can be awaited/merged before final result; `process_ms` no longer mirrors `prefetch_ms`.

- [x] P1 DB-side missing-embedding query
  Owner: Sub-agent Plato (`019d0f72-46fa-7431-908d-de0707d2c87f`)
  Files: `crates/ferrumyx-ingestion/src/repository.rs`
  Success criteria: `find_chunks_without_embeddings` avoids full paper chunk load (with compatibility fallback).

- [x] P1 Ingestion audit logging for dedupe
  Owner: Sub-agent Plato (`019d0f72-46fa-7431-908d-de0707d2c87f`)
  Files: `crates/ferrumyx-ingestion/src/repository.rs`
  Success criteria: dedupe paths emit `ingestion_audit` rows with action+detail.

- [x] P1 Database initialize path optimization
  Owner: Sub-agent Carson (`019d0f72-46df-7173-a7fe-17e0366bfa58`)
  Files: `crates/ferrumyx-db/src/database.rs`
  Success criteria: `initialize()` computes existing tables once and creates only missing tables.

- [x] P1 Synonyms query correctness fix
  Owner: Sub-agent Carson (`019d0f72-46df-7173-a7fe-17e0366bfa58`)
  Files: `crates/ferrumyx-db/src/entities.rs`
  Success criteria: no `array_contains` on `Utf8` synonyms field; synonym matching remains functional.

- [x] P1 Batch entity-name lookup to reduce N+1
  Owner: Sub-agent Carson (`019d0f72-46df-7173-a7fe-17e0366bfa58`)
  Files: `crates/ferrumyx-db/src/entities.rs`, `crates/ferrumyx-web/src/handlers/targets.rs`, `crates/ferrumyx-web/src/handlers/ranker.rs`
  Success criteria: handlers use batch name resolution, minimizing per-row `find_by_id` calls.

- [x] P2 Perf smoke checks + regression testing
  Owner: Main agent
  Files: as needed (tests/bench commands, no scope conflict)
  Success criteria: compile + targeted tests pass; measured latency/throughput gains for changed hotspots where benchmarkable.

- [x] P2 Documentation sync notes
  Owner: Main agent
  Files: response output and optional docs patch
  Success criteria: list of docs that remain stale and required updates.

## Validation Evidence

- `cargo test -p ferrumyx-db -p ferrumyx-ranker -p ferrumyx-ingestion` passed.
- `cargo test -p ferrumyx-web --no-run` passed.
- `cargo run -p ferrumyx-web --bin perf_micro` results:
  - Entity lookup (`3000` IDs): `N+1=24506ms`, `batch=191ms`, `128.30x` faster.
  - Missing embeddings (`8000` chunks): `old_scan=92ms`, `db_filter=87ms`, `1.06x` faster.
  - Embedding writes (`200` chunks): `per_row=86552ms`, `bulk=435ms`, `198.97x` faster.

## Documentation Notes

- `ARCHITECTURE.md` should be updated to explicitly document:
  - ranker bounded TTL caches and provider-refresh bounded concurrency behavior,
  - ingestion heavy-lane drain toggle (`FERRUMYX_INGESTION_HEAVY_LANE_DRAIN`) and telemetry semantics,
  - batch entity-name lookup path used by web handlers.
- `README.md` should include how to run `perf_micro` and interpret baseline speedups for local verification.

## Pass 2 Additions (2026-03-21)

- [x] Ranker allocation/micro-optimization pass
  Owner: Sub-agent Beauvoir (`019d0f72-46c1-7601-9acc-5d94579a2078`)
  Files: `crates/ferrumyx-ranker/src/lib.rs`
  Outcome: reduced transient lowercase/context allocations in fact-loop keyword checks; added preallocations for candidate/ranking maps and vectors.

- [x] DB/web hot-path micro-optimization pass
  Owner: Sub-agent Carson (`019d0f72-46df-7173-a7fe-17e0366bfa58`)
  Files: `crates/ferrumyx-db/src/database.rs`, `crates/ferrumyx-web/src/handlers/targets.rs`, `crates/ferrumyx-web/src/handlers/ranker.rs`
  Outcome: table-name snapshot reuse in `stats()`, reduced response vector reallocations, and one-time normalized gene filter preparation.

- [x] Ingestion clone-reduction + DB-filter regression fix
  Owner: Sub-agent Plato (`019d0f72-46fa-7431-908d-de0707d2c87f`) + Main agent integration
  Files: `crates/ferrumyx-ingestion/src/pipeline.rs`, `crates/ferrumyx-ingestion/src/repository.rs`
  Outcome: fewer `PaperMetadata` clones in prefetch path, restored DB-side missing-embedding query path, persisted dedupe audit events.

## Pass 3 Additions (2026-03-21)

- [x] Batch chunk embedding update path
  Owner: Main agent
  Files: `crates/ferrumyx-db/src/chunks.rs`, `crates/ferrumyx-ingestion/src/repository.rs`
  Outcome: bulk embedding updates now use chunk-ID keyed lookup + batch merge-upsert; removes per-item read/delete/reinsert pattern from bulk path.

- [x] Benchmark extension for embedding-write throughput
  Owner: Main agent
  Files: `crates/ferrumyx-web/src/bin/perf_micro.rs`
  Outcome: added `per_row` vs `bulk` embedding-write micro-benchmark to quantify write-path gain.

## Out-of-scope for this sprint (tracked, not implemented unless time permits)

- [ ] Normalize `ent_compounds.target_gene_ids` and `ent_pathways.gene_members` into link tables (remove `LIKE` scans)
- [ ] Provider refresh distributed scheduler/coalescer
- [ ] Full perf CI gate with reproducible benchmark thresholds
